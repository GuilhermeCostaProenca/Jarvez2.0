# Jarvez Frontend

Interface web do Jarvez (Next.js + LiveKit).

## Rodar

```powershell
pnpm install
pnpm dev
```

Variaveis em `.env.local`:

- `LIVEKIT_API_KEY`
- `LIVEKIT_API_SECRET`
- `LIVEKIT_URL`
- `AGENT_NAME` (opcional)
